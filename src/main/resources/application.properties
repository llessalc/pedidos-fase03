spring.datasource.url=jdbc:mysql://${MYSQL_HOST}:3306/food_58?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=${MYSQL_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl


# Filas Publisher
events.queue-producao=${PEDIDOS_PRODUCAO}
events.queue-pedido-finalizado=${PEDIDOS_FINALIZADOS}
events.queue-pedidos-criados=${PEDIDOS_QUEUE}

# Filas Consumer
events.queue-confirmacao-pagamento=${PAGAMENTOS_CONFIRMADOS_QUEUE}
events.queue-pagamento-cancelado=${PAGAMENTOS_CANCELADOS_QUEUE}



pagamento.service=${pagamento_service}

